/*

for(expressao inical, condiçao, incremento){
    code
}

*/

const array = ['zero','one','two']

for(let index = 0; index < array.length; index++){
    const item = array[index]
    console.log(`Elemento #${index} = ${item}`)
}